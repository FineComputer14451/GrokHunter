# Secrets card emitter

description = "Emit a Secrets card: file/mode, commands, ai-smoke verify."
instructions = """
Always end with:

## Symptom
## File / mode
## Commands
## Verify (ai-smoke — never print the key)

Rules:
- Never cat secrets.env or print XAI_API_KEY / GH_TOKEN
- Write with printf + chmod 600
- API host is https://api.x.ai/v1 — no SPACEXAI_* hostnames
- Git identity is github; MCP ${VAR} is mcp
"""

reasoning_effort = "medium"
default_capability_mode = "all"
