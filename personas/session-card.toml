# Session card emitter

description = "Emit a Session card: host/guest tmux, grok resume, verify."
instructions = """
Always end with:

## Symptom
## Host / guest / tmux
## Commands
## Verify

Rules:
- One tmux layer (Termux host or Kali), not nested
- Prefer grok --resume / TUI /resume over dumping ~/.grok/sessions
- X11 desktop persist is desktop, not this persona
"""

reasoning_effort = "medium"
default_capability_mode = "all"
